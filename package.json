{
  "name": "@woxqaq/pi-web",
  "version": "0.2.5",
  "description": "Web Shell for pi coding agent",
  "keywords": [
    "pi",
    "pi-coding-agent",
    "pi-package"
  ],
  "license": "MIT",
  "repository": {
    "url": "https://github.com/woxQAQ/pi-web"
  },
  "files": [
    "dist/bin",
    "dist/bridge",
    "web-dist",
    "README.md"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "pnpm run build:server && pnpm run build:web",
    "build:bin": "pnpm -C packages/bin build",
    "build:bridge": "pnpm -C packages/bridge build",
    "build:server": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && pnpm run build:bridge && pnpm run build:bin",
    "build:svelte": "pnpm -C packages/svelte build",
    "build:vue": "pnpm -C packages/web build",
    "build:web": "pnpm run build:svelte",
    "check:type": "tsgo --noEmit",
    "check:web": "pnpm -C packages/svelte check",
    "dev:pi": "PI_BRIDGE_PORT=${PI_BRIDGE_PORT:-8080} pi -e ./packages/bin/index.ts",
    "dev:pi:inspect": "PI_BRIDGE_PORT=${PI_BRIDGE_PORT:-8080} NODE_OPTIONS='--inspect-brk' pi -e ./packages/bin/index.ts",
    "dev:svelte": "pnpm -C packages/svelte dev",
    "dev:vue": "pnpm -C packages/web dev",
    "dev:web": "pnpm run dev:svelte",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "prepack": "pnpm run build",
    "smoke:pi-web": "node scripts/smoke-pi-web.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "@lobehub/icons-static-svg": "^1.88.0",
    "@mariozechner/pi-coding-agent": "^0.65.2",
    "@pi-web/bridge": "workspace:*",
    "@types/node": "^24.3.0",
    "@typescript/native-preview": "7.0.0-dev.20260413.1",
    "oxfmt": "^0.45.0",
    "oxlint": "^1.60.0",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*"
  },
  "engines": {
    "node": ">=20.6.0"
  },
  "pi": {
    "extensions": [
      "./dist/bin/index.js"
    ]
  }
}
